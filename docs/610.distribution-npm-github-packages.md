# 610. 배포 절차 — GitHub Releases + npm wrapper (GitHub Packages)

이 포크의 네이티브 산출물(iOS xcframeworks + Android AAR)을 외부 소비자
(주: cirqle-mobile)에게 배포하는 표준 절차.

## 0. 구조

```
GitHub Releases (kyehyukahn/wallet-core)
  └─ v<X.Y.Z>/
      ├─ WalletCoreCommon.xcframework.zip
      ├─ WalletCoreRs.xcframework.zip
      ├─ wallet-core.aar
      └─ SHA256SUMS

GitHub Packages npm registry (npm.pkg.github.com)
  └─ @kyehyukahn/wallet-core@X.Y.Z
      └─ 얇은 wrapper (postinstall이 위 Release에서 다운+검증+풀기)
```

- 산출물 바이너리는 **Releases**가 호스팅 (~390 MB / 버전).
- npm 패키지는 KB 단위의 wrapper만 담는다 (postinstall.js + index.js + 메타).
- 소스 리포 `kyehyukahn/wallet-core`가 PUBLIC이므로 publish되는 패키지도 자동 PUBLIC.

## 1. 사전 준비 (1회)

### 1.1 발행자 측 인증

```bash
# ~/.npmrc 에 GitHub Packages 토큰 등록
TOKEN=$(gh auth token)        # write:packages 스코프 포함 권장
echo "//npm.pkg.github.com/:_authToken=${TOKEN}" >> ~/.npmrc
```

`gh auth login` 시 토큰 스코프에 `write:packages`가 포함돼 있어야 한다.
부족하면 `gh auth refresh -s write:packages`.

### 1.2 빌드 환경

[docs/600.build-prerequisites.md](600.build-prerequisites.md) 참고. 첫
빌드 시 30분 정도 소요.

## 2. 새 버전 절차 (반복) — 자동화

`v0.1.0`까지는 수동으로 수행했고, `v0.1.1`부터는 **태그 push 한 번**으로
GitHub Actions가 빌드 + Release + npm publish를 자동 처리한다.
자동화 워크플로우는 [`.github/workflows/release.yml`](../.github/workflows/release.yml),
설계 근거는 [612.design-ci-release-automation.md](612.design-ci-release-automation.md).

### Step 1: pre-flight 커밋 (메인테이너 로컬에서)

세 가지 변경을 dev 브랜치에 미리 커밋해야 한다. CI가 시작과 동시에
일치 여부를 검증하므로 누락 시 즉시 fail.

```bash
# 1-1. npm-package/package.json 의 "version"을 새 버전과 일치하도록 수정
$EDITOR npm-package/package.json   # "version": "0.1.1"

# 1-2. Release notes 작성
$EDITOR docs/release-notes/v0.1.1.md   # v0.1.0 노트를 템플릿으로

# 1-3. 위 두 변경을 dev로 commit + push (보통 PR 거쳐서 머지)
git add npm-package/package.json docs/release-notes/v0.1.1.md
git commit -m "chore(release): prepare v0.1.1"
git push
```

### Step 2: 태그 + push

```bash
git tag -a v0.1.1 -m "v0.1.1"
git push origin v0.1.1
```

이 시점부터 CI가 동작한다 — Actions 페이지에서 진행 상황 확인.

### Step 3: 자동으로 일어나는 일 (`release.yml`)

워크플로우는 macOS Apple Silicon 러너에서 약 15~30분 소요.

| 단계 | 동작 |
|---|---|
| 검증 | 태그 형식 / `npm-package/package.json` version / release notes 존재 |
| 빌드 | Rust+C++ 의존성 → iOS xcframework → Android AAR |
| 패키징 | `tools/release-pack ${VERSION}` |
| Release | `softprops/action-gh-release@v2`로 태그에 자산 4개 부착 |
| npm publish | GitHub Packages npm 레지스트리에 `@kyehyukahn/wallet-core@${VERSION}` 발행 |
| Job summary | Actions 실행 페이지에 Release URL / npm URL / 산출물 크기 표시 |

성공 후 자동 게시 결과:
- GitHub Release: `https://github.com/kyehyukahn/wallet-core/releases/tag/v<VERSION>`
- npm 패키지: 동일 버전이 GitHub Packages에 PUBLIC으로 발행

### Step 4: 첫 publish 후 1회만 — 패키지 가시성 PUBLIC 전환

신규 메이저 시리즈가 아닌 같은 패키지의 후속 버전은 가시성이 유지된다.
이미 `@kyehyukahn/wallet-core`가 PUBLIC이라면 v0.1.0 셋업 단계는 재실행 불필요.

### 수동 fallback

CI가 일부 단계에서 실패해도 메인테이너가 로컬에서 보완할 수 있다.

```bash
# Release 자산 빌드만 실패한 경우: 로컬에서 빌드 후 동일 태그에 자산 추가
tools/release-pack 0.1.1
gh release upload v0.1.1 build/release-v0.1.1/*

# npm publish만 실패한 경우 (Release는 정상)
cd npm-package && npm publish
```

동일 태그로 워크플로우 재실행은 GitHub Actions UI의 "Re-run jobs" 또는
`workflow_dispatch`로 가능. 자산은 덮어쓰기, npm은 이미 발행된 버전이면
정상적으로 fail.

## 3. 소비자 측 셋업 (참고)

**GitHub Packages npm은 패키지 가시성과 무관하게 install 시 항상 인증 토큰을
요구한다** (npmjs.org와의 결정적 차이). 따라서 소비자 프로젝트 `.npmrc`에는
스코프 매핑 + 인증 토큰 **두 줄 모두** 필요하다:

```
# 소비자 프로젝트 루트 .npmrc
@kyehyukahn:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
```

`${GITHUB_TOKEN}`은 `read:packages` 스코프만 있으면 충분하다. 개인 개발자는
`gh auth token`(또는 GitHub Personal Access Token)을 셸 환경 변수로 export
하거나 `.npmrc`에 평문으로 넣는다. CI는 `secrets.GITHUB_TOKEN`을 그대로 사용.

> 참고: `.npmrc`에 평문 토큰을 넣을 경우 그 파일을 절대 commit하지 말 것
> (root `.gitignore`에 `.npmrc` 또는 `.npmrc.local` 등록 권장).

설치:

```bash
pnpm add @kyehyukahn/wallet-core@^0.1.0
```

postinstall이 `https://github.com/kyehyukahn/wallet-core/releases/download/v<version>/`
에서 산출물을 받아 `node_modules/@kyehyukahn/wallet-core/native/` 아래에
배치한다. SHA256SUMS를 받아 각 파일 무결성을 검증한다.

Release 자산 자체(`.zip` / `.aar` / `SHA256SUMS`)는 PUBLIC 리포의 Release이므로
**익명으로 다운로드 가능** (postinstall도 토큰 없이 가져옴). 인증은 npm 레지스트리에서
패키지 메타데이터를 조회·tarball을 받을 때만 필요하다.

## 4. 자주 묻는 것

**Q. 패키지를 public으로 바꿔도 토큰이 필요한 이유?**
A. GitHub Packages npm 레지스트리는 가시성과 무관하게 항상 인증을 요구한다
(2026년 기준). npmjs.org만 익명 install이 가능. 이 제약을 피하려면 npmjs.org로
이전 publish하거나, fetch 스크립트 방식으로 우회.

**Q. 첫 publish 후 패키지가 private으로 만들어지는 이유?**
A. GitHub Packages 개인 계정 npm 패키지의 초기 가시성은 `private`이 기본이다
(repo 가시성과 자동 동기화되지 않는다). 첫 publish 후
`https://github.com/users/<owner>/packages/npm/package/<name>` →
Package settings → Danger zone → Change visibility에서 수동으로 public 전환.

**Q. 빌드 산출물을 git에 직접 넣지 않는 이유?**
A. xcframework / AAR 슬라이스가 GitHub의 파일당 100 MB 제한을 초과하고,
LFS는 1 GB 무료 quota를 빠르게 소진한다. Releases는 산출물 호스팅에
최적화되어 있고 별도 비용 없이 대용량을 다룬다.
[docs/603.build-ios.md](603.build-ios.md) / [604.build-android.md](604.build-android.md)
의 산출물 크기 절 참고.

**Q. cirqle 이외의 소비자가 늘면?**
A. 동일 메커니즘으로 동일 패키지에 의존하면 된다. 향후 정책상
조직 자산화가 필요해지면 리포를 적절한 GitHub 조직으로 transfer.

## 5. 자동화 — 완료

- `.github/workflows/release.yml`: `v*.*.*` 태그 push 또는 수동 dispatch로 빌드 → release-pack → GitHub Release → npm publish 자동화 완료 (v0.1.1부터 적용). 설계 근거는 [612.design-ci-release-automation.md](612.design-ci-release-automation.md).
- 첫 빌드는 ~25~35분, 캐시 적중 시 ~12~18분 (Swatinem/rust-cache + `actions/cache` for `build/local`).

향후 추가 후보:
- 캐시 워밍 cron (정규 PR이 적을 때 캐시가 만료되는 경우)
- 빌드 산출물에 대한 추가 서명(sigstore/cosign) — 현재는 SHA256SUMS로 무결성만 보장
